name: first-exercise-workflow
on: [push, pull_request]
jobs:
  shell-cmd-job:
    runs-on: ubuntu-latest
    steps:
      - name: python|pip version check
        run: |
          python --version
          pip --version
